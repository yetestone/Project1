#include<stdio.h>
#include<stdlib.h>
main ()
{
/*	float xf, yf, zf;
	xf = 123.123457;
	yf = 123.123456;
	zf = xf - yf;
	if (zf == 0)printf("Zero!\n");
	printf("x=%f,y=%f,z=%f\n", xf, yf, zf);
	system("pause");
*/
/*	int x, y;
	x = 10;
	y = -3;
	printf("x+y=%d\n", x + y);
	printf("x-y=%d\n", x - y);
	printf("x*y=%d\n", x*y);
	printf("x/y=%d\n", x / y);
	printf("x%%y=%d\n", x%y);
	system("pause");
*/
/*	int a = 1;
	a--;
	printf("%d\n", a);
*/

/*		int x = 10;
		int y = 10;
		x = x * 2;//使用简单赋值语句实现x乘以2。
		y *= 2;//使用复合赋值语句实现y乘以2。
		printf("x=%d\n", x);
		printf("y=%d\n", y);

	int height = 185;
		if (height >= 180)
		{
			printf("恭喜小明可以进入篮球队");
		}

	int year = 2014;
		if (year % 4 == 0)
		{
			printf("今年是闰年");
	}
		else
		{
			printf("今年是平年");
		}*/
	int sale = 120;
	int year = 1;
	if (sale > 100)
	{
		if (year >= 2)
		{
			printf("优秀员工奖\n");
		}
		else
		{
			printf("%s\n", "很遗憾。期望你再接再厉");
		}
	}
	else
	{
		printf("%s\n", "很遗憾。期望你再接再厉");
	}
		system("pause");

}